# workspace files are ignored
*.dws
*.code-workspace

# The dll file can be gotten at https://github.com/raysan5/raylib/releases/tag/5.0
# make sure it's 5.0
raylib.dll
libraylib.*

# built from https://github.com/Brian-ED/temp-c-raylib
libtemp-c-raylib.so

# Some testing Brian's doing with C
ffi.c

# Tests for seeing how ⎕NA communicates with specific arguments
breakapl/zig-out
breakapl/.zig-cache

# Output for zig files, since dyalog doesn't have a way of seeing STDOUT easily
breakapl/junk_file.txt